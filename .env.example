# Starlink Dashboard Configuration

# Operating mode configuration (choose ONE of the two approaches below):
#
# Approach 1: STARLINK_MODE (recommended, explicit)
#   STARLINK_MODE=simulation    # Offline development with simulated data
#   STARLINK_MODE=live          # Real Starlink terminal data
#
# Approach 2: SIMULATION_MODE (backward compatible)
#   SIMULATION_MODE=true        # Simulation mode (equivalent to STARLINK_MODE=simulation)
#   SIMULATION_MODE=false       # Live mode (equivalent to STARLINK_MODE=live)
#
# If both are set, STARLINK_MODE takes precedence.
# Default: simulation mode for safe offline development
STARLINK_MODE=simulation

# Starlink Dish Configuration (for live mode)
# IP address or hostname of the Starlink dish on the local network
# Default: 192.168.100.1 (factory default for Starlink)
STARLINK_DISH_HOST=192.168.100.1

# gRPC port for Starlink dish communication
# Default: 9200 (standard Starlink gRPC port)
STARLINK_DISH_PORT=9200

# Prometheus metrics retention period
# Valid units: y, w, d, h, m, s (e.g., 15d, 7d)
PROMETHEUS_RETENTION=15d

# Grafana admin user password
# IMPORTANT: Change this in production!
GRAFANA_ADMIN_PASSWORD=admin

# Service port mappings
# Backend FastAPI service port
STARLINK_LOCATION_PORT=8000

# Prometheus metrics scraper port
PROMETHEUS_PORT=9090

# Grafana dashboard UI port
GRAFANA_PORT=3000

# Timezone configuration for world clocks on dashboards
# Use IANA timezone identifiers (e.g., America/Los_Angeles, Europe/London, Asia/Tokyo)
TIMEZONE_TAKEOFF=America/Los_Angeles
TIMEZONE_LANDING=Europe/London
